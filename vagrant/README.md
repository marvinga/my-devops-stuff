# Vagrant

# -> install 


# fix repo

```
sudo cd /etc/yum.repos.d/; sudo sed -i 's/mirrorlist/#mirrorlist/g' /etc/yum.repos.d/CentOS-*; sudo sed -i 's|#baseurl=http://mirror.centos.org|baseurl=http://vault.centos.org|g' /etc/yum.repos.d/CentOS-*; sudo yum update -y
```

# -> ad-hoc commands


# -> track

6/8/2023
- Decided to finish linkedin just viewing the videos and do labs only from book (when I buy it)
